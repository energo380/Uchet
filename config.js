/* ============================================================
   АДРЕС СЕРВЕРА
   ------------------------------------------------------------
   Единственное, что нужно здесь поменять - строка внизу.
   Вставьте между кавычками адрес веб-приложения Apps Script,
   тот, что оканчивается на /exec.

   ВНИМАНИЕ: это адрес НОВОГО проекта, с узлами учёта.
   Адрес от стажировок сюда не подходит: там другая таблица
   и другой набор действий.

   Этот файл создаётся один раз. Обновление index.html его
   не затрагивает, вписывать адрес заново не придётся.

   Адрес меняется только в одном случае: если в Apps Script
   нажать «Начать развёртывание» и создать НОВОЕ развёртывание.
   Если обновлять существующее (Управление развёртываниями ->
   карандаш -> Версия: Новая), адрес остаётся прежним и трогать
   этот файл не нужно.
   ============================================================ */

const API_URL = 'СЮДА_ВСТАВИТЬ_АДРЕС_ОКАНЧИВАЮЩИЙСЯ_НА_EXEC';
